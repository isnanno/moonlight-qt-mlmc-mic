# Receptor MLMC — Host / VM

> **Use o guia simplificado:** [`LEIA-ME.md`](LEIA-ME.md)  
> Este arquivo existe só para desenvolvimento no repositório.

O pacote de release contém apenas:

- `INSTALAR.bat` — instalação completa
- `INICIAR.bat` — teste manual
- `REMOVER-AUTOSTART.bat` — remover do boot

Scripts legados (`install-deps.bat`, `install-autostart.bat`, etc.) foram substituídos pelo fluxo único `INSTALAR.bat` → `setup.ps1`.
